---
const {date} = Astro.props;

const options = { year: 'numeric', month: 'long', day: 'numeric' };
const formattedDate = new Intl.DateTimeFormat('zh-CN', options).format(Date.parse(date))
---
<time datetime={date}>{formattedDate}</time>